## Customize the test machine
machine:
   # Set the timezone
  timezone:
    America/Edmonton

  # Version of Node to use
  node:
    version:
      6.5.0

## Customize dependencies
dependencies:
  pre:

    - npm run clean #"npm cache clean && npm run rimraf -- node_modules doc coverage dist"
    - npm install --global webpack
    - npm install --global webpack-dev-server
    - npm install --global karma-cli
    - npm install --global protractor
    - npm install --global typescript
    - npm uninstall webpack --save-dev
    - npm install webpack@2.1.0-beta.21 --save-dev
    - npm uninstall "@types/selenium-webdriver" --save-dev
    - npm install "@types/selenium-webdriver@2.44.26" --save-dev
    - npm install 
  override:
    - npm run build:prod



## Customize test commands
test:
  override:
    - npm run test

## Custom notifications
##notify:
##  webhooks:
    # A list of hashes representing hooks. Only the url field is supported.
##    - url: https://circleci.com/hooks/github
